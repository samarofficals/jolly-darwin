# Usery OS Pacman Configuration

[options]
HoldPkg     = pacman glibc
Architecture = auto
Color
CheckSpace
ParallelDownloads = 5
SigLevel    = Required DatabaseOptional
LocalFileSigLevel = Optional

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[multilib]
Include = /etc/pacman.d/mirrorlist

# Usery OS Custom Package Repository
# (Commented out during ISO build until GitHub Pages repo is fully published)
# [usery]
# SigLevel = Optional TrustAll
# Server = https://usery-project.github.io/usery-repo/x86_64
